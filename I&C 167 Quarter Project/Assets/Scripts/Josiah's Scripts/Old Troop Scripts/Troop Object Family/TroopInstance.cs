using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TroopInstance : MonoBehaviour                                        // Code written by Josiah Lomax
{
    // Start is called before the first frame update
    void Start()
    {
                                                                                // This script will be responsible for handling the initialization of a troop object within the game
                                                                               // Will likely determine which troop type to initialize 
                                                                              // Will reference the SOTroopDefinition script, to specify the scriptable object troop type

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
