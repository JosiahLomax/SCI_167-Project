using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Shielder : MonoBehaviour                                                   // Code written by Josiah Lomax
{
    // Start is called before the first frame update
    void Start()
    {
                                                                                      // This script will be attached to the Shielder prefab
                                                                                      // Will control the specific behaviour of the Shielder troop
                                                                                      // Will likely assign the data from the Shielder scriptable object
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
